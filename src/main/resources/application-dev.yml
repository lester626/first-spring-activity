server:
  port: 8081

spring:
  application:
    name: Application Dev

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:E:/demo
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect